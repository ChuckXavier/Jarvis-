# JARVIS V3 — dependencies
# NOTE: alpaca-py is the CORRECT, maintained SDK and is what every import in
# execution/engine.py uses. The rebuild spec's instruction to install the
# deprecated `alpaca-trade-api` was an error; do not "fix" this backward.

# Core data stack
pandas>=2.1
numpy>=1.26
scipy>=1.11

# Market & macro data
yfinance>=0.2.18
fredapi>=0.5.0

# NEW in V3: constituent fetch (config/universe.py) — requests for the HTTP
# call, lxml for pandas.read_html table parsing.
requests>=2.31
lxml>=4.9

# Database (PostgreSQL on Railway)
sqlalchemy>=2.0
psycopg2-binary>=2.9

# Broker (paper trading)
alpaca-py>=0.21

# Scheduling / runtime
apscheduler>=3.10
loguru>=0.7
python-dotenv>=1.0

# API / health endpoint
fastapi>=0.104
uvicorn>=0.24

# Dashboard (V2 monitor — unchanged)
streamlit>=1.28
